请使用我项目里的 N8N_BASE_URL 与 N8N_API_KEY，直接调用 n8n 的 REST API，修复“新闻采集/News Ingest”工作流中 HTTP Request (V3) 节点的表达式错误（invalid syntax），并做一次 dryRun 测试。步骤如下，所有结果与关键响应请贴出：

【0】准备
- 从 Replit Secrets 读取：
  N8N_BASE_URL、N8N_API_KEY、NEWS_INGESTION_SECRET、NEWS_CHANNEL_ID、TELEGRAM_BOT_TOKEN（仅用于验证，不打印值）。
- 以 Bearer {{N8N_API_KEY}} 调用 n8n API。

【1】定位工作流与节点
- GET {{N8N_BASE_URL}}/rest/workflows 列出所有工作流，找到名字包含 “新闻采集” 或 “News Ingest” 的那个，记下 {workflowId}。
- GET {{N8N_BASE_URL}}/rest/workflows/{workflowId} 取到工作流 JSON。
- 在 nodes[] 里定位类型为 "n8n-nodes-base.httpRequest" 且名称包含 “Ingest” 的 V3 节点（通常就是推送到 /api/news/ingest 的那一只）。

【2】修正 HTTP Request (V3) 节点参数（重点：表达式必须用 {{ ... }}，不要有 ={{ ... }})
- 维持 Method=POST，URL 使用现有值（若为空请设为我服务的公开地址 + "/api/news/ingest"）。
- Request Format 设为 JSON（jsonParameters = true，sendBody = true）。
- Headers 只保留两条：
  1) Name: X-News-Secret
     Value: {{ $env.NEWS_INGESTION_SECRET }}
     （如果 $env 取不到，兜底：{{ $json.news_secret || $json.NEWS_INGESTION_SECRET }}）
  2) Name: Content-Type
     Value: application/json   （纯文本，不用表达式）
- Body 映射为 JSON 字段（全部用表达式），不要 raw 字符串：
  title: {{ $json.title }}
  url: {{ $json.link }}
  source: {{ $json.source || "WSJ" }}
  isoDate: {{ $json.isoDate }}
  content: {{ $json.content || $json["content:encoded"] || $json.contentSnippet }}
  tier: {{ $json.tier }}
  domain: {{ $json.domain }}
  paid: {{ Array.isArray($json.categories) && $json.categories.includes("_PAID") }}
  dryRun: {{ true }}   // 先干跑
- 移除一切旧 Header/Body 中含 `={{...}}` 的字段，确保 V3 只剩 {{ ... }} 语法。

【3】保存工作流
- PATCH {{N8N_BASE_URL}}/rest/workflows/{workflowId} 提交更新后的 workflow JSON（只改该 HTTP 节点相关字段）。
- 如果工作流处于 “inactive”，请保持原状态不改动是否激活；只更新配置。

【4】执行一次 dryRun
- 用 POST {{N8N_BASE_URL}}/rest/workflows/{workflowId}/run 触发一次 “单条输入” 运行，传入一个样例 item（就是我提供的 WSJ 记录）：
  {
    "title": "Stocks Sink in Broad AI Rout Sparked by China's DeepSeek",
    "link": "https://www.wsj.com/articles/nikkei-may-rise-as-weak-yen-raises-earnings-hopes-776a8056?mod=rss_markets_main",
    "isoDate": "2025-01-27T19:26:00.000Z",
    "source": "WSJ",
    "content": "U.S. stocks were mostly lower, ... Nvidia was down 16%.",
    "contentSnippet": "U.S. stocks were mostly lower, ... Nvidia was down 16%.",
    "categories": ["_PAID"],
    "tier": 4,
    "domain": "wsj.com"
  }
- 回传本次运行中 Ingest 节点的：
  - 最终渲染后的 Headers（展开后值，隐藏敏感值）
  - 最终渲染后的 JSON Body
  - 目标服务返回的状态码与响应体前 200 字
  - 若响应体包含 { ok, dryRun } 字段，请展示它们

【5】若 $env.NEWS_INGESTION_SECRET 取不到
- 先尝试从 Replit Secrets 读该值注入到环境；若 n8n 无法读取 env，则创建一个上游 “Set” 节点注入 `news_secret` 字段，Ingest Header 使用 {{ $json.news_secret }} 作为兜底。
- 重新保存并再次 dryRun。

【6】输出最终结果
- 打印 “HTTP V3 节点修复完成：headers 表达式=OK、body=OK、dryRun 状态码=XXX”
- 若有错误，请给出节点参数的最终 JSON 片段（只与 headers/body/format 相关的部分），方便我核验。
