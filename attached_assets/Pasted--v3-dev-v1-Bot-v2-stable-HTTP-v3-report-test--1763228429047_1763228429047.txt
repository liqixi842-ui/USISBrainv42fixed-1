请帮我在 v3-dev 环境中实现一个“研报功能 v1（测试版）”，只在开发Bot上生效，不影响 v2-stable。目标是：

- HTTP: /v3/report/test   → 返回静态示例研报 JSON
- HTTP: /v3/report/:symbol → 根据股票代码生成简易研报（先用轻量AI + 简单数据）
- Telegram dev Bot: 新增 /report 命令，调用 v3-dev 研报服务

具体分 3 步做：

────────────────────────
【第1步】创建研报路由和服务（HTTP 层）
────────────────────────
1）在 v3_dev/routes/ 目录中新建文件：report.js  
内容结构大致如下（你按Node/Express最佳实践来写，具体代码由你实现）：

- 创建一个 Express Router
- GET /test
  - 返回 JSON 示例，比如：
    {
      "ok": true,
      "env": "v3-dev",
      "type": "equity_research_report_mock",
      "symbol": "AAPL",
      "sections": {
        "summary": "...",
        "business": "...",
        "valuation": "...",
        "technical": "...",
        "risks": "..."
      }
    }
- GET /:symbol
  - 从 req.params.symbol 读取股票代码（如 aapl / AAPL / tsla）
  - 先做一个非常简单的实现（v1 测试版）：
    - 尝试调用现有的内部函数获取行情（如果项目里已有 symbol normalize / quote service，可以复用；如果没有，就先用一个 mock / 占位数据，比如 price=123.45）
    - 调用一个 v3_dev/services/reportService.js 里的函数 buildSimpleReport(symbol, basicData)
    - 返回 JSON：
      {
        ok: true,
        env: "v3-dev",
        symbol: "<上面解析出来的symbol>",
        generated_at: ISO时间,
        report: {
          title: "...",
          rating: "WATCH" | "BUY" | "HOLD" | "SELL",
          horizon: "短期/中期/长期",
          summary: "...",
          drivers: [ "...", "..." ],
          risks: [ "...", "..." ],
          technical_view: "...",
          comment: "这是v1测试版，仅用于开发环境"
        }
      }

2）在 v3_dev/services/ 目录中新建文件：reportService.js  
实现一个基础版本的研报生成逻辑（不要求完美，只要可用）：

- 导出函数：async function buildSimpleReport(symbol, basicData)
- v1 实现建议（你自由发挥）：
  - 先根据 basicData 组一个 prompt，调用 OpenAI 或项目中已集成的任意一个轻量模型（例如 GPT-4o-mini 或 DeepSeek），但请确保：
    - 所有调用都只在 v3-dev 内部使用，不要影响现有 orchestrate
    - 可以加一个超时保护（例如 10~15 秒）
  - 如果你暂时不想接 AI，可以先返回一个 hard-coded 模板，用 symbol 和 price 拼接几句说明
- 无论是否调用 AI，最终统一返回一个 JS 对象，供路由层直接使用

3）更新 v3_dev/routes/index.js  
- 把新的 report 路由挂载进去：
  - const reportRouter = require('./report');
  - router.use('/report', reportRouter);
- 确认最终的挂载路径是：
  - /v3/report/test
  - /v3/report/:symbol

完成后，请自行用 curl 或浏览器验证：

- GET /v3/report/test
- GET /v3/report/AAPL

并在回复里把返回的 JSON 一并发给我看。

────────────────────────
【第2步】让开发Bot支持 /report 指令（只在 dev_bot 生效）
────────────────────────
1）打开 v3_dev/services/devBotHandler.js  
请在现有逻辑基础上：

- 为开发Bot增加一个新的命令处理：
  - 当用户发送：
    - "/report"
    - "/report AAPL"
    - "/report tsla"
  - 解析出股票代码（缺省时可返回提示信息）
- 处理思路建议（你按结构实现）：
  - 如果没有带代码：回复一条消息，比如：
    "请用格式：/report AAPL"
  - 如果带了代码：
    - 调用内部 HTTP: GET http://localhost:3000/v3/report/:symbol
      或者直接调用 reportService.buildSimpleReport(symbol, basicData)
    - 从返回结果中提取几个关键字段，组合成一条适合 Telegram 的文本，例如：
      - 标题：USIS·研报测试版（v3-dev）
      - 标的：AAPL
      - 评级：WATCH（测试版）
      - 简要观点（1–3行）
      - 风险提示（1–2行）
      - 末尾注明：本功能仅在开发Bot(v3-dev)中测试，不影响生产系统
- 确保这些逻辑 **只在 devBot 中生效**，不要修改生产Bot的 handler 代码

2）为了避免生产环境污染，请确保：
- 不修改任何 v2-stable 的 bot 逻辑文件
- 所有新代码均位于 v3_dev/ 目录下
- 生产 Bot 收不到 /report 命令的特殊处理（收到也当普通文本或原逻辑处理）

────────────────────────
【第3步】自检与状态回报
────────────────────────
完成以上步骤后，请帮我做三个自检，并把结果用文字+必要的JSON片段返回给我（不要省略字段）：

1）HTTP 自检：
- GET https://liqixi888.replit.app/v3/report/test
- GET https://liqixi888.replit.app/v3/report/AAPL

2）Telegram 开发Bot 自检：
- 给 dev bot 发送：
  - "/test"
  - "/report"
  - "/report AAPL"
- 把 dev bot 的回复内容原样返回给我

3）确认生产Bot未受影响：
- 给生产Bot发送之前常用的分析命令（例如 /analyze AAPL 或你现有命令）
- 确认一切如旧，没有新“研报测试”字段出现

最后，请给我一个简短的小结，说明：
- v3-dev 研报功能 v1 是否已上线？
- 是否确认只在开发Bot和 /v3 路径生效？
